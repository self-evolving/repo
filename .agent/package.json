{
  "name": "@self-evolving/sepo",
  "version": "0.3.1",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc",
    "bootstrap:memory": "node dist/cli/memory/bootstrap-branch.js",
    "test": "npm run test:ci",
    "test:ci": "npm run build && npm run test:runtime && npm run check:workflow && npm run check:docs && npm run check:shell",
    "test:runtime": "node --test $(find dist -name '*.test.js' -type f ! -name 'docs-validation.test.js' ! -name 'workflow-action-validation.test.js' ! -name 'envelope.test.js' ! -name 'agent-action-expiration.test.js' ! -name '*-shell.test.js' | sort)",
    "check:workflow": "node --test dist/__tests__/envelope.test.js dist/__tests__/workflow-action-validation.test.js",
    "check:docs": "node --test dist/__tests__/docs-validation.test.js",
    "check:shell": "bash -n $(find scripts ../.github/actions -name '*.sh' -type f | sort) && node --test dist/__tests__/agent-action-expiration.test.js $(find dist -name '*-shell.test.js' -type f | sort) $(find scripts -path '*/test/*.test.cjs' -type f | sort)"
  },
  "dependencies": {
    "@zed-industries/codex-acp": "0.15.0",
    "acpx": "^0.6.1",
    "yaml": "^2.8.3"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.7.0"
  }
}
